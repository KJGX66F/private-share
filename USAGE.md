使用教程

文本模式

选择“文本模式”，填写内容，密码可留空，也可任意长度。选择有效期并生成密链。

文件模式

选择“文件模式”，可点击或拖拽多个文件，也可同时填写文字说明。

V6：最多 30 个文件；单文件代码上限 2 GB；单次分享总量代码上限 4 GB。

小文件

约 20 MB 以内会尽量提前缓存到浏览器内存，避免之后文件路径失效。

大文件

大文件采用 8 MB 分片，上传期间请不要移动、删除原文件，也不要断开 U 盘或网络盘。

存储模式

自动：推荐，小分享 KV，大分享 R2

仅 KV：文本 / 小文件

仅 R2：视频 / 大文件

KV+R2：双存备份，消耗两份存储与更多操作

密码

有密码：PBKDF2-SHA256 派生 AES Key。

无密码：随机 AES Key 放在完整 URL #k=... 中。必须分享完整链接。

图片 / PDF / 视频

图片：预览、查看大图、下载

PDF：在线预览、下载

视频：原生 <video controls> 播放、下载

浏览器是否能播放视频取决于容器和编码。

大文件下载

Chrome / Edge 等支持 File System Access API 时，可逐分片下载、解密、写入磁盘。其他浏览器会回退为 Blob 下载，超大文件会占更多内存。
